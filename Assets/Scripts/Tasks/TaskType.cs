namespace Tasks
{
    /// <summary>
    /// Each level will have own group of task`s type
    /// I think 10 tasks for a level is enough
    /// </summary>
    public enum TaskType
    {
        None = 0,

        // Temporary names - please, rename it before use
        FirstTask = 1,
        SecondTask,

        TaskInNextLevel = 10,




        // Should be removed later
        TemporaryType = 999
    }
}
